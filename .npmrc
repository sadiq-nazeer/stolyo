# Only allow build scripts for packages listed in package.json pnpm.onlyBuiltDependencies
# This provides better security by only allowing trusted packages to run build scripts
ignore-dep-scripts=true