import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.energetickiwizoom',
  appName: 'energetic-kiwi-zoom',
  webDir: 'dist'
};

export default config;
